// App uses plain CSS (no Tailwind). Local config so the PostCSS loader
// does not walk up the directory tree and pick up an ancestor config.
const config = {
  plugins: {},
};

export default config;
